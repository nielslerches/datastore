# datastore
An in-memory datastore, written in Python

## Features
- Two-phase commit
- Optional indexing
- Inserts (`.add()`)
  - `.update()` at some point
  - Same for `.delete()`

## Why
Don't know, really.
